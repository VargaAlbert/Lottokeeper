import style from "./scss/Footer.module.scss";

const Footer = () => {
    return (
        <section className={style.mainContainer}>
            Footer
        </section>
    );
}

export default Footer;