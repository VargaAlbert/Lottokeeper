
import style from "./UserHitResult.module.scss";

const UserHitResult: React.FC = () => {
    return (
        <section className={style.mainContainer}>
            UserHitResult
        </section>
    );
}

export default UserHitResult;