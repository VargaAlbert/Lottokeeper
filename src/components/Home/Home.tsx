import style from "./Home.module.scss";

const Home: React.FC = () => {
    return (
        <section className={style.mainContainer}>
            Home
        </section>
    );
}

export default Home;